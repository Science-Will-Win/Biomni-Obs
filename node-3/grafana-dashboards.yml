apiVersion: 1

providers:
  - name: "Default Dashboards"
    orgId: 1
    folder: "LLM Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # 컨테이너 내부의 경로 (아래 docker-compose.yml에서 마운트할 위치)
      path: /var/lib/grafana/dashboards
